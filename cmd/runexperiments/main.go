package main

func main() {

	// Expect command-line arguments: flag whether
	// this is our prototype or a comparison system,
	// GCloud project identifier, GCloud service
	// account, GCloud Storage bucket, file system
	// location to store results.

	// Prepare local results folder.

	// Create PKI with PREMIUM network tier
	// and decent hardware specs.

	// Verify it is running.

	// Ingest GCloud configuration file.

	// Iterate over configuration slice and
	// execute a gcloud util command for each line.

	// Verify all instances are running.

	// Wait for all instances to signal that
	// they have fetched all evaluation artifacts
	// from resources server.

	// If zeno: send PKI signal to start.

	// Spin-query GCloud bucket until we see
	// measurement files present from all nodes.

	// Download all files from GCloud bucket
	// to prepared local experiment folder.
}
